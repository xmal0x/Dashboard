export interface Message {
    from: string
    message: string
    date: string
}

export interface SummaryData {
    id: number
    title: string
    value: string
    direction: string
    diff: string
}
