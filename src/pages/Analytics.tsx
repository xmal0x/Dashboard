import React from 'react';
import {PageWrapper} from "../hoc";

const Analytics = () => {
    return (
        <div>
            Analytics
        </div>
    );
};

export default PageWrapper(Analytics, 'Analytics')
