import React from 'react';
import {PageWrapper} from "../hoc";

const Messages = () => {
    return (
        <div>
            Messages
        </div>
    );
};

export default PageWrapper(Messages,'Messages')
