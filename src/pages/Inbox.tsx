import React from 'react';
import {PageWrapper} from "../hoc";

const Inbox = () => {
    return (
        <div>
            Inbox
        </div>
    );
};

export default PageWrapper(Inbox, 'Inbox')
