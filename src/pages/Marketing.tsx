import React from 'react';
import {PageWrapper} from "../hoc";

const Marketing = () => {
    return (
        <div>
            Marketing
        </div>
    );
};

export default PageWrapper(Marketing, 'Marketing')
